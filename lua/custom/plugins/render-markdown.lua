return {
  'MeanderingProgrammer/render-markdown.nvim',
  lazy = true, -- lazy load the plugin
  ft = { 'markdown', 'codecompanion' }, -- apply to markdown and codecompanion buffers
  config = true,
}
